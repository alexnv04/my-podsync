[server]
port = 8080
hostname = "https://my-podsync.onrender.com"
data_dir = "/app/data"

[storage]
  [storage.local]
  data_dir = "/app/data"

[tokens]
# Podsync loads tokens from PODSYNC_YOUTUBE_API_KEY environment variable

[feeds]
  [feeds.frmathew]
  url = "https://www.youtube.com/channel/UCsEfdsXejpne93addKoOhIw"
  page_size = 2
  update_period = "1h"
  quality = "high"
  format = "audio"
  clean = { keep_last = 2 }
  custom_args = [
    "--extractor-args", "youtube:player_client=ios,android"
  ]
